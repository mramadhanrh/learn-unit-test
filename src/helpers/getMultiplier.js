/**
 *
 * @param {number} value value that will be multiplied to
 * @param {number} multiplier multiple value by "multiplier" times
 *
 * @example
 * func(10, 5) will return 50
 */
const getMultiplier = (value, multiplier) => value * multiplier;

export default getMultiplier;
