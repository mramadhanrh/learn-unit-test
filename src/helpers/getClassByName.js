/**
 *
 * @param {string} className Class name that available in classlist member
 * @param {array} classList An array object containing { index, name, url }
 *
 * @example
 *
 * className: barbarian || Barbarian || barBaRiAN
 * classList: [{ index: "barbarian", name: "Barbarian", url: "/api/classes/barbarian" }, { index: "bard", name: "Bard", url: "/api/classes/bard" }];
 */

const getClassByName = (className, classList) => {
  return null;
};

export default getClassByName;
